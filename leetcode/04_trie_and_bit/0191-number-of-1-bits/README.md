# 191. Number of 1 Bits

## 我的思考
循环统计 `1` 的数量即可。

使用 `n &= n - 1` 可以每次消去二进制表示中的一个 `1`，  
记录消去了多少个 `1`，就等价于统计 `1` 的数量，并且效率更高一些。

## 卡点
无

## 关键点
- `n &= n - 1` 每次会消去最低位的一个 `1`

## 复杂度
- 时间：O(number of 1 bits)
- 空间：O(1)

## 注意
无